# OS
.DS_Store
**/.DS_Store

# Python
__pycache__/
**/__pycache__/
*.pyc
.venv/

# Logs & scratch
*.log
error.txt
*.swp
# analyze_ioe_files() dumps this into the cwd on every run, so it appears
# wherever the SUPPA tests happen to be run from. Regenerated, never edited.
ioe_all_junctions.csv
SESSION_HANDOFF_CONTEXT.md
/a
tests/x.py

# Local docs, figures, archives (not source)
*.docx
*.pptx
*.key
*.pdf
*.xlsx
*.tar
domain_*.png
domas_paper/

# Large full-run golden (slow test) — kept local only, regenerate on demand
# (~93MB across its two CSVs). The test bootstraps it on the first --run-slow
# run and compares against it thereafter, so a fresh clone needs no fixture.
# Both names: the case directory was renamed, and the stale rule silently let
# the file back into the repo.
tests/reference_outputs/leafcutter_full/
tests/reference_outputs/leafcutter_full__representative_True/
alphafold_benchmark/repro_data/

# run_examples.sh default output directory
run_examples_output/
